syntax = "proto3";

package messages;

import "types.proto";

// The generic service definition.
service GrpcUnaryInputterService {
    // Accepts a message but doesn't return anything back
    rpc MakeRequest (ProtoMessage) returns (Empty) {
    }
}