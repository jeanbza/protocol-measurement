syntax = "proto3";

package messages;

import "types.proto";

// The generic service definition.
service GrpcStreamingInputterService {
    // Accepts a message but doesn't return anything back
    rpc MakeRequest (stream ProtoMessage) returns (Empty) {
    }
}